# Korean Python TODO

이 문서는 이 프로젝트를 실제 언어 프로젝트처럼 키우기 위한 작업 목록이다.

철학:

- Python의 문법과 의미를 그대로 따른다.
- 한국어는 표현만 편하게 해주는 역할로 둔다.
- 새 문법은 최소한으로 추가한다.
- 애매한 자연어 해석은 하지 않는다.

---

## 1. 언어 방향 고정

- [ ] 프로젝트 철학을 한 문장으로 확정한다.
- [ ] 이 프로젝트가 "새 언어"인지 "Python 한글화 도구"인지 확정한다.
- [ ] 지원할 문법과 지원하지 않을 문법의 경계를 정한다.
- [ ] `SPEC.md`를 만들어 문법 기준을 문서로 고정한다.
- [ ] `README.md`와 `SPEC.md`의 설명이 서로 충돌하지 않게 정리한다.

## 2. 핵심 문법 안정화

- [ ] 한글 키워드와 Python 키워드의 대응표를 확정한다.
- [ ] 함수 정의와 함수 호출이 깨지지 않게 유지한다.
- [ ] 문자열 안의 단어가 키워드로 오인되지 않게 보장한다.
- [ ] `반복 N번:` 문법을 안정적으로 처리한다.
- [ ] `만약 x 는 3 보다 크면:` 같은 조건문 확장을 안정화한다.
- [ ] 들여쓰기 기반 블록 구조가 Python과 동일하게 나오도록 맞춘다.
- [ ] 복잡한 자연어 문법은 추가하지 않는다.

## 3. 전처리/트랜스파일러 정리

- [ ] `Transpiler.py`의 책임을 명확히 한다.
- [ ] 토큰 기반 치환과 줄 기반 치환의 역할을 분리한다.
- [ ] 공백이 잘못 들어가는 문제를 재발하지 않게 한다.
- [ ] 토큰 위치 정보가 유지되는지 검증한다.
- [ ] 필요한 경우 전처리 단계와 토큰 치환 단계를 분리한다.
- [ ] `parser_1.py`가 실제로 필요한지 판단한다.
- [ ] 중복 기능이 있으면 하나로 통합한다.

## 4. 실행 흐름 개선

- [ ] `main.py`를 현재 CLI 진입점으로 유지한다.
- [ ] 기본 입력 파일과 출력 파일 규칙을 고정한다.
- [ ] `--out` 옵션 사용 방식을 문서화한다.
- [ ] `--run` 옵션의 동작을 명확히 한다.
- [ ] 생성된 `.py` 파일을 바로 실행하는 흐름을 안정화한다.
- [ ] 입력 파일이 없을 때의 메시지를 명확하게 만든다.

## 5. 에러 메시지 개선

- [ ] Python 예외가 그대로 나가도 원인 파악이 가능하게 정리한다.
- [ ] 트랜스파일 단계에서 잡을 수 있는 오류는 미리 잡는다.
- [ ] 입력 누락, 파일 없음, 문법 오류 메시지를 구분한다.
- [ ] 사용자에게 보이는 메시지는 한국어로 통일한다.
- [ ] 필요한 경우 `kpy` 전용 오류 메시지를 만든다.

## 6. 테스트와 예제

- [ ] `examples/` 폴더를 만든다.
- [ ] 기본 출력 예제를 추가한다.
- [ ] 입력 예제를 추가한다.
- [ ] 함수 정의/호출 예제를 추가한다.
- [ ] 조건문 예제를 추가한다.
- [ ] 반복문 예제를 추가한다.
- [ ] 문자열 내부 텍스트 보존 예제를 추가한다.
- [ ] 번역 결과가 예상과 같은지 비교하는 테스트를 만든다.
- [ ] 예제를 바꿀 때마다 재확인할 기준을 만든다.

## 7. 문서 정리

- [ ] `README.md`를 사용자 안내서 수준으로 다듬는다.
- [ ] `SPEC.md`를 언어 명세서처럼 유지한다.
- [ ] 각 키워드의 의미를 표로 정리한다.
- [ ] 예제와 실제 출력 결과를 함께 보여준다.
- [ ] 사용자가 처음 보는 기준으로도 이해되게 설명을 정리한다.

## 8. 향후 확장

- [ ] 리스트와 딕셔너리 관련 보조 문법을 검토한다.
- [ ] `import` 관련 문법 지원 범위를 정한다.
- [ ] 클래스 문법 지원 여부를 결정한다.
- [ ] 예외 처리 문법 지원 범위를 정한다.
- [ ] 포맷터나 검사 도구가 필요한지 판단한다.
- [ ] 최종적으로 독립적인 언어 도구로 볼 수준인지 다시 평가한다.

---

## 우선순위

1. 언어 방향 고정
2. 핵심 문법 안정화
3. 전처리/트랜스파일러 정리
4. 실행 흐름 개선
5. 에러 메시지 개선
6. 테스트와 예제
7. 문서 정리
8. 향후 확장

