# Korean Python Spec

## 1. 프로젝트 개요

Korean Python은 한글 키워드로 작성한 `.kpy` 파일을 일반 Python 코드로 변환하는 트랜스파일러 프로젝트다.

목표는 다음과 같다.

- 한글에 익숙한 사람이 Python을 더 쉽게 작성할 수 있게 한다.
- `.kpy` 파일을 안정적으로 Python 코드로 변환한다.
- 변환된 코드는 일반 Python 인터프리터에서 실행 가능해야 한다.

이 프로젝트는 인터프리터가 아니라 **트랜스파일러**다.

---

## 2. 설계 원칙

1. 문법은 단순해야 한다.
2. 변환 결과는 가능한 한 Python 문법과 1:1 대응되어야 한다.
3. 기존 Python의 동작을 최대한 유지한다.
4. 한글 문법은 “새로운 언어”가 아니라 “Python의 한글 표현”이어야 한다.
5. 불명확한 문법은 억지로 추측하지 않는다.

---

## 3. 입력과 출력

### 입력
- 확장자: `.kpy`
- 인코딩: UTF-8
- 입력 예시:
  - `hello.kpy`

### 출력
- 확장자: `.py`
- 인코딩: UTF-8
- 기본 출력 파일명은 입력 파일과 같은 이름의 `.py` 파일이다.
- 예: `hello.kpy` → `hello.py`

---

## 4. 지원 문법

### 4.1 키워드 치환
다음 한글 키워드는 Python 키워드로 변환된다.

| 한글 | Python |
|---|---|
| 출력 | print |
| 입력 | input |
| 정수 | int |
| 실수 | float |
| 문자열 | str |
| 리스트 | list |
| 튜플 | tuple |
| 딕셔너리 | dict |
| 집합 | set |
| 불 | bool |
| 참 | True |
| 거짓 | False |
| 비어있음 | None |
| 만약 | if |
| 다른 | elif |
| 그렇지않으면 | else |
| 반복 | for |
| 동안 | while |
| 함수 | def |
| 반환 | return |
| 클래스 | class |
| 시도 | try |
| 예외발생 | except |
| 최종적으로 | finally |
| 불러오다 | import |
| 프롬 | from |
| 글로벌 | global |
| 그리고 | and |
| 또는 | or |
| 아님 | not |
| 델 | del |
| 멈춤 | break |
| 계속 | continue |
| 통과 | pass |
| 범위 | range |
| 길이 | len |
| 타입 | type |
| 열거 | enumerate |
| 압축 | zip |
| 정수로 | int |
| 실수로 | float |
| 문자열로 | str |
| 절대값 | abs |
| 최대 | max |
| 최소 | min |
| 합계 | sum |
| 비동기 | async |
| 기다려 | await |
| 매치 | match |
| 경우 | case |

---

## 5. 문법 규칙

### 5.1 함수 정의
```kpy
함수 인사(이름):
    출력("안녕, " + 이름)
```

변환 결과:
```python
def 인사(이름):
    print("안녕, " + 이름)
```

### 5.2 함수 호출
```kpy
인사("철수")
```

변환 결과:
```python
인사("철수")
```

### 5.3 조건문
```kpy
만약 x 는 3 보다 크면:
    출력("큼")
```

변환 결과:
```python
if x > 3:
    print("큼")
```

### 5.4 반복문
```kpy
반복 3번:
    출력("안녕")
```

변환 결과:
```python
for _ in range(3):
    print("안녕")
```

### 5.5 입력
```kpy
이름 = 입력()
나이 = 정수(입력())
```

변환 결과:
```python
이름 = input()
나이 = int(input())
```

---

## 6. 현재 지원하는 특수 문법

현재 변환기는 아래의 자연어식 표현을 일부 지원한다.

### 6.1 `반복 N번:`
- `반복 3번:` 형태는 `for _ in range(3):`로 변환된다.

### 6.2 `만약 A 는 B 보다 크면:`
- `만약 x 는 3 보다 크면:`은 `if x > 3:`로 변환된다.
- `작으면`도 지원한다면 `if x < 3:`로 변환된다.

---

## 7. 비지원 범위

다음은 현재 명세에서 보장하지 않는다.

- 복잡한 자연어 문장 전체 해석
- 자동 타입 추론
- 한글 변수명 자동 변환
- 문자열 내부의 단어를 키워드로 치환하는 동작
- 모든 Python 문법의 한글화
- 완전한 한국어 문장 기반 프로그래밍

---

## 8. 에러 처리 원칙

1. 파이썬 문법 오류는 가능한 한 원본 코드 위치를 유지한 채 보여준다.
2. 트랜스파일 단계에서 알 수 있는 문제는 Python 실행 전에 발견하는 것이 이상적이다.
3. 입력 누락, 파일 없음, 잘못된 경로는 명확하게 출력한다.
4. 불명확한 자동 수정은 하지 않는다.

---

## 9. 실행 방식

기본 실행 흐름:

1. `.kpy` 파일 읽기
2. 한글 키워드 및 문법 변환
3. `.py` 파일 저장
4. 필요하면 바로 실행

CLI 예시:

```bash
python main.py hello.kpy
python main.py hello.kpy -o build/hello.py
python main.py hello.kpy --run
```

---

## 10. 테스트 기준

다음 예제는 최소한 통과해야 한다.

- `출력("안녕")` → `print("안녕")`
- `함수 이름(값):` → `def 이름(값):`
- `반복 3번:` → `for _ in range(3):`
- `만약 x 는 3 보다 크면:` → `if x > 3:`
- `정수(입력())` → `int(input())`
- 문자열 안의 한글 텍스트는 변환되지 않아야 한다.

---

## 11. 구현 우선순위

1. 토큰 보존 정확도 확보
2. 함수 호출 형태 유지
3. 조건문/반복문 자연어 문법 확장
4. 에러 메시지 개선
5. 테스트 예제 추가
6. `parser_1.py` 정리 여부 결정

---

## 12. 향후 확장 아이디어

- `출력 줄바꿈 없이(...)`
- `만약`, `아니면` 더 자연스러운 문법 추가
- 리스트/딕셔너리 리터럴 보조 문법
- 사용자 친화적인 문법 오류 메시지
- 자동 포맷터 추가
